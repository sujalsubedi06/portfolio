import type { Certification } from '@/types';

// Do not invent certifications. Add entries here only as they are actually completed.
export const certifications: Certification[] = [];

export const certificationsEmptyMessage = 'Certifications will be added as they are completed.';
